import { createSelector } from 'reselect';

import { initialState } from './reducer';

// const selectShowcasesDomain = state => state.showcases || initialState;

// const select{{ properCase name }} = () => createSelector(select{{ properCase name }}domain, subState => subState);

// export { select{{ properCase name }}, select{{ properCase name }}domain };