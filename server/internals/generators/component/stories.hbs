/* eslint-disable import/no-extraneous-dependencies */
import React from 'react';
import { Meta, Story } from '@storybook/react/types-6-0';
import  { {{properCase name}} } from './index';

import { I{{pascalCase name}} } from './{{pascalCase name}}';

// eslint-disable-next-line arrow-parens
const Template: Story<I{{pascalCase name}}.IProps> = (args) => <{{properCase name}} {...args} />;

export const {{pascalCase name}}Story = Template.bind({});

export default {
  title: 'Components/{{properCase name}}',
  component: {{properCase name}},
} as Meta;


{{pascalCase name}}Story.args = {
    /* the args you need here will depend on your component */
}